require.config({
    deps : [ 'tests/runner' ],

    paths : {
        // JavaScript folders
        lib : '../lib',
        tests : '../tests',
        exams : '../exams',

        // Libraries
        jquery : '../lib/jquery'
    }
});
