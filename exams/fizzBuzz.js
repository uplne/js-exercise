if (typeof define !== 'function') { var define = require('amdefine')(module); }

define(function() {
    return {
        /**
         * Write a method which returns:
         *
         * 'fizz' if parameter passed is divisible by 3
         * 'buzz' if parameter passed is divisible by 5
         * 'fizzbuzz' if parameter passed is divided by 3 and 5
         * number
         * false if no number is provided or paramter passed is not a number
         */
        fizzBuzz: function(num) {

        }
    };
});
