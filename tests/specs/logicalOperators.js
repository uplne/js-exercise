if (typeof define !== 'function') { var define = require('amdefine')(module); }
if (typeof expect !== 'function') { var expect = require('expect.js'); }

define([
    '../../exams/arrays'
], function(exam) {
    'use strict';

    describe('LogicalOperators', function() {

    });
});
